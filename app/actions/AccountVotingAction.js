/**
 * Created by cuiyujie on 2018/10/8.
 */
import alt from "alt-instance";

class AccountVotingActions {
    addItem(collection, item_id) {
        return item_id;
    }
    removeItem(collection, item_id) {
        return item_id;
    }

    addBudgets(name) {
        return name;
    }

    removeBudgets(name) {
        return name;
    }

    addCommittees(name) {
        return name;
    }

    removeCommittees(name) {
        return name;
    }

    addWitnesses(name) {
        return name;
    }

    removeWitnesses(name) {
        return name;
    }

    addWorkers(name) {
        return name;
    }

    removeWorkers(name) {
        return name;
    }

    addProxy(name) {
        return name;
    }

    removeProxy(name) {
        return name;
    }
}

export default alt.createActions(AccountVotingActions);
